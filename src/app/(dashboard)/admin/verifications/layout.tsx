// frontend/src/app/(dashboard)/admin/verifications/layout.tsx

import React from 'react';

export default function AdminVerificationsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}