// frontend/src/app/(dashboard)/admin/services/layout.tsx

import React from 'react';

export default function AdminServicesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}