// frontend/src/app/(dashboard)/admin/transactions/layout.tsx

import React from 'react';

export default function AdminTransactionsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}