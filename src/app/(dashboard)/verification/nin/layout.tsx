// frontend/src/app/(dashboard)/verification/nin/layout.tsx

import React from 'react';

export default function NINVerificationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}