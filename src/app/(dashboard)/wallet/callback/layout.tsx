// frontend/src/app/(dashboard)/wallet/callback/layout.tsx

import React from 'react';

export default function PaymentCallbackLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}