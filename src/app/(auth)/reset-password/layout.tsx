// frontend/src/app/(auth)/reset-password/layout.tsx

import React from 'react';

export default function ResetPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}